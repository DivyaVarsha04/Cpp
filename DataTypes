int dataTypes(string type) {
	
	cin>>type;
	if(type==Integer) 
		cout<<4;
	else if(type==Long) 
		cout<<8;
	else if(type==Float)
		cout<<4;
	else if(type==Double)
		 cout<<8;
	else
	 cout<<1;
	return 0;
}
